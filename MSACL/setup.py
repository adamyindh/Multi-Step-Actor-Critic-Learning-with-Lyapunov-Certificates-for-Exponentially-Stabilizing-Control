from setuptools import setup, find_packages

setup(
    name="MSACL",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # List project dependencies (e.g., "numpy>=1.20", "torch>=1.10")
    ],
    python_requires=">=3.9",  # Minimum Python version requirement
)